import { type SVGProps } from 'react';

/**
 * Пропсы для компонента LockIcon.
 */
export interface LockIconProps extends SVGProps<SVGSVGElement> {
  /**
   * Размер иконки в пикселях.
   */
  size?: number;
}

