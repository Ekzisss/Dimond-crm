import { type SVGProps } from 'react';

/**
 * Пропсы для компонента EmailIcon.
 */
export interface EmailIconProps extends SVGProps<SVGSVGElement> {
  /**
   * Размер иконки в пикселях.
   */
  size?: number;
}

