import { type SVGProps } from 'react';

/**
 * Пропсы для компонента EyeIcon.
 */
export interface EyeIconProps extends SVGProps<SVGSVGElement> {
  /**
   * Размер иконки в пикселях.
   */
  size?: number;
}

